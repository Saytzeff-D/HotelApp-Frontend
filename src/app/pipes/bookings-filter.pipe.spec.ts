import { BookingsFilterPipe } from './bookings-filter.pipe';

describe('BookingsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BookingsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
